import styled from "styled-components";

export const Container = styled.button`
    height: 3rem;
    background: var(--blue-300);
    border: 0;
    border-radius: 1rem;
    color: var(--white);
    font-weight: 500;
    cursor:pointer;

    a {
        align-items: center;
        padding: 2rem ;
    }
`